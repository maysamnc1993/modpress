# 🎨 سیستم آیکون‌های سفارشی

## 📚 3 روش برای تغییر آیکون‌ها:

---

### 🎯 روش 1: Lucide Icons (پیشنهادی)

**مدرن‌ترین و تمیزترین آیکون‌ها**

```php
// در init.php
$icon_type = 'lucide';
```

**ویژگی‌ها:**
- ✅ 1000+ آیکون
- ✅ خیلی تمیز و مینیمال
- ✅ SVG خالص
- ✅ سبک و سریع
- ✅ Open Source

**مثال:**
```javascript
'dashboard' => 'layout-dashboard'
'posts' => 'file-text'
'media' => 'image'
'users' => 'users'
```

**لیست کامل:** https://lucide.dev/icons/

---

### 🎯 روش 2: Font Awesome (محبوب)

**محبوب‌ترین کتابخانه آیکون**

```php
// در init.php
$icon_type = 'fontawesome';
```

**ویژگی‌ها:**
- ✅ 2000+ آیکون رایگان
- ✅ خیلی محبوب
- ✅ پشتیبانی عالی
- ✅ همه جا استفاده میشه

**مثال:**
```css
dashboard: '\f015' /* home */
posts: '\f303' /* file-lines */
media: '\f03e' /* image */
users: '\f0c0' /* users */
```

**لیست کامل:** https://fontawesome.com/icons

---

### 🎯 روش 3: SVG سفارشی (پیشرفته)

**آیکون‌های اختصاصی خودت**

```php
// در init.php
$icon_type = 'svg';
```

**ویژگی‌ها:**
- ✅ کنترل کامل
- ✅ آیکون‌های اختصاصی
- ✅ هیچ وابستگی خارجی
- ✅ بهینه‌ترین حالت

**نحوه افزودن آیکون جدید:**

```php
// در svg-icons.php
$this->icons['my-icon'] = '<svg>...</svg>';
```

---

## 🔧 نصب و راه‌اندازی:

### گام 1: انتخاب نوع آیکون

در فایل `init.php` خط 51:

```php
$icon_type = 'lucide'; // یکی از: lucide, fontawesome, svg, none
```

### گام 2: فعال‌سازی ماژول

ماژول به صورت خودکار فعال میشه.

---

## 🎨 سفارشی‌سازی:

### اضافه کردن آیکون جدید به Lucide:

```php
// در lucide-icons.php
const iconMap = {
    'menu-icon-custom': 'star', // آیکون دلخواه
};
```

### اضافه کردن آیکون جدید به Font Awesome:

```php
// در custom-icons.php
body.admin-theme-active #adminmenu .menu-icon-custom .wp-menu-image::before {
    content: '\f005' !important; /* star */
}
```

### اضافه کردن SVG سفارشی:

```php
// در svg-icons.php
$this->icons['custom'] = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
    <path d="M12 2L2 22h20L12 2z"/>
</svg>';
```

---

## 📋 لیست آیکون‌های پیش‌فرض:

| منو | Lucide | Font Awesome | توضیح |
|-----|--------|--------------|-------|
| پیشخوان | `layout-dashboard` | `\f015` | Dashboard |
| نوشته‌ها | `file-text` | `\f303` | Posts |
| رسانه | `image` | `\f03e` | Media |
| برگه‌ها | `file` | `\f15c` | Pages |
| دیدگاه‌ها | `message-square` | `\f075` | Comments |
| پوسته | `palette` | `\f53f` | Appearance |
| افزونه‌ها | `plug` | `\f1e6` | Plugins |
| کاربران | `users` | `\f0c0` | Users |
| ابزارها | `wrench` | `\f0ad` | Tools |
| تنظیمات | `settings` | `\f013` | Settings |

---

## 🎯 افزونه‌های محبوب:

| افزونه | Lucide | Font Awesome |
|--------|--------|--------------|
| WooCommerce | `shopping-cart` | `\f07a` |
| محصولات | `package` | `\f291` |
| سفارشات | `shopping-bag` | `\f0f2` |
| Contact Form 7 | `mail` | `\f0e0` |
| Elementor | `layers` | `\f1fc` |
| Yoast SEO | `search` | `\f002` |

---

## 💡 نکات:

1. **Lucide** برای ظاهر مدرن و تمیز
2. **Font Awesome** برای سازگاری و تنوع
3. **SVG** برای کنترل کامل و بهینه‌سازی

---

## 🔄 تغییر بین سیستم‌ها:

فقط کافیه در `init.php` مقدار `$icon_type` رو تغییر بدی:

```php
$icon_type = 'lucide';      // Lucide Icons
$icon_type = 'fontawesome'; // Font Awesome
$icon_type = 'svg';         // SVG های سفارشی
$icon_type = 'none';        // Dashicons پیش‌فرض
```

---

## 🎨 رنگ‌بندی آیکون‌ها:

آیکون‌ها به صورت خودکار با رنگ‌بندی تم هماهنگ میشن:

```css
/* عادی */
color: var(--admin-text-body);

/* Hover */
color: var(--admin-primary);

/* Active */
color: var(--admin-primary);
```

---

**انتخاب با توئه! 🎨**
