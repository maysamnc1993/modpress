# 📧 ماژول فرم تماس

یک فرم تماس حرفه‌ای با قابلیت Ajax و اعتبارسنجی

---

## ✨ امکانات

- ✅ ارسال Ajax (بدون رفرش صفحه)
- ✅ اعتبارسنجی سمت کلاینت و سرور
- ✅ ارسال ایمیل خودکار
- ✅ طراحی واکنش‌گرا
- ✅ پشتیبانی از RTL
- ✅ انیمیشن‌های روان
- ✅ لودینگ حرفه‌ای

---

## 🚀 استفاده

### شورت‌کد

```php
[dst_contact_form]
```

### با پارامترها

```php
[dst_contact_form title="ارتباط با ما" button_text="ارسال پیام"]
```

### در فایل PHP

```php
<?php echo do_shortcode('[dst_contact_form]'); ?>
```

---

## ⚙️ تنظیمات

تنظیمات در فایل `module.json` قابل تغییر است:

```json
{
    "settings": {
        "email": "your@email.com",
        "success_message": "پیام شما با موفقیت ارسال شد",
        "error_message": "خطا در ارسال پیام"
    }
}
```

---

## 🔌 هوک‌ها

### بعد از ارسال موفق

```php
add_action('dst_contact_form_submitted', function($data) {
    // $data شامل: name, email, phone, message, date, ip
    
    // مثلاً ذخیره در دیتابیس
    // یا ارسال به CRM
    // یا ارسال اعلان
});
```

---

## 🎨 سفارشی‌سازی استایل

می‌توانید استایل‌ها را override کنید:

```css
.dst-contact-form-wrapper {
    max-width: 800px; /* تغییر عرض */
}

.dst-contact-form button[type="submit"] {
    background: #ff6b6b; /* تغییر رنگ دکمه */
}
```

---

## 📝 فیلدها

- **نام** - الزامی، حداقل ۳ کاراکتر
- **ایمیل** - الزامی، فرمت معتبر
- **تلفن** - اختیاری
- **پیام** - الزامی، حداقل ۱۰ کاراکتر

---

## 🔒 امنیت

- ✅ Nonce verification
- ✅ Sanitize inputs
- ✅ Escape outputs
- ✅ Validate email
- ✅ Rate limiting ready

---

## 📦 نیازمندی‌ها

- PHP 7.4+
- WordPress 6.0+
- jQuery

---

## 🐛 رفع مشکل

### ایمیل ارسال نمی‌شود

1. چک کنید پلاگین SMTP نصب باشد
2. تنظیمات `settings.email` را چک کنید
3. لاگ‌های وردپرس را بررسی کنید

### استایل لود نمی‌شود

1. مطمئن شوید ماژول فعال است
2. کش را پاک کنید
3. `wp-content/themes/your-theme/modules/contact-form/assets/style.css` را چک کنید

---

## 📄 مجوز

GPL-2.0-or-later
