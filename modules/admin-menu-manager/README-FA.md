# 📋 ماژول مدیریت منوی ادمین

نسخه 4.0.0

## 📌 توضیحات

این ماژول منوهای پنل مدیریت وردپرس را به صورت منظم و کاربرپسند سازماندهی می‌کند.

## 🎯 ترتیب منوها

```
┌─────────────────────────────┐
│  📊 پیشخوان                 │
│  🧩 ماژول‌ها                │
│  ⚙️ تنظیمات وب‌سایت         │
├─────────────────────────────┤
│  📝 نوشته‌ها                │
│  🖼️ رسانه                   │
│  📄 برگه‌ها                  │
│  📋 فهرست‌ها                │
├─────────────────────────────┤
│  👥 کاربران                 │
├─────────────────────────────┤
│  📁 سایر                    │
│    └─ نظرات                │
│    └─ ظاهر                  │
│    └─ افزونه‌ها             │
│    └─ ابزارها              │
│    └─ تنظیمات              │
│    └─ ...                  │
└─────────────────────────────┘
```

## ✨ ویژگی‌ها

### 1. سازماندهی منوها
- ترتیب منطقی و کاربرپسند
- جداکننده‌های بصری بین بخش‌ها
- منوی "سایر" برای دسترسی به همه بخش‌ها

### 2. باز شدن با کلیک
- زیرمنوها فقط با کلیک باز می‌شوند (نه hover)
- تجربه کاربری بهتر در موبایل و دسکتاپ

### 3. صفحه تنظیمات وب‌سایت
- دسترسی سریع به تنظیمات هدر و فوتر
- لینک مستقیم به Customizer
- مدیریت هویت سایت

### 4. صفحه سایر
- نمایش گرید تمام منوهای اضافی
- آیکون‌های مناسب برای هر بخش
- دسترسی سریع با یک کلیک

## 📁 ساختار فایل‌ها

```
admin-menu-manager/
├── module.json          # تنظیمات ماژول
├── init.php             # فایل اصلی
├── README-FA.md         # مستندات
├── assets/
│   ├── css/
│   │   └── menu-style.css
│   └── js/
│       └── menu-click.js
└── includes/
    └── theme-settings.php
```

## 🔧 نحوه استفاده

ماژول به صورت خودکار فعال می‌شود و نیازی به تنظیمات ندارد.

### تنظیمات وب‌سایت
از منوی `تنظیمات وب‌سایت` می‌توانید به موارد زیر دسترسی داشته باشید:
- هدر و فوتر
- هویت سایت (لوگو و عنوان)
- سفارشی‌ساز کامل
- ابزارک‌ها

### دسترسی به سایر بخش‌ها
از منوی `سایر` می‌توانید به تمام بخش‌های دیگر وردپرس دسترسی داشته باشید:
- نظرات
- ظاهر (تم‌ها)
- افزونه‌ها
- ابزارها
- تنظیمات وردپرس
- و سایر پلاگین‌ها...

## 🎨 سفارشی‌سازی

برای تغییر ترتیب منوها، فایل `init.php` را ویرایش کنید:

```php
// تغییر موقعیت یک منو
$this->restore_menu_item($original_menu, 'edit.php', 10);
//                                        ^ slug      ^ position
```

## 📝 تغییرات نسخه 4.0.0

- بازنویسی کامل کد
- حذف حالت‌های ساده/کامل (فقط یک ترتیب استاندارد)
- بهبود صفحه سایر با نمایش گرید
- اضافه شدن صفحه تنظیمات وب‌سایت
- بهبود استایل‌ها و UX
